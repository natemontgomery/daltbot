<% if errors.any? %>
  <output class="text-danger">
    <h2><%= pluralize(errors.count, "error") %> stopped your weather:</h2>

    <ul>
      <% errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </output>
<% end %>
